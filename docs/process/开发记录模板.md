# {功能名称} 开发记录

> **Agent编号**：{Agent编号}
> **开发日期**：{YYYY-MM-DD}
> **Git Commit**：{commit hash}
> **Git分支**：{feature分支名}

---

## 1. 开发目标

{描述本次开发要实现的功能点，引用任务书中的对应章节。}

**关联任务书**：`/workspace/lis-project/reports/templates/{Agent编号}-{模块名}-开发任务书.md` 第{X}节

---

## 2. 实现方案

### 2.1 技术方案

{描述采用的技术方案和实现思路。}

### 2.2 核心逻辑

{描述核心业务逻辑和关键算法。}

### 2.3 数据库变更

{如有数据库变更，列出DDL语句。}

```sql
-- {变更说明}
{SQL语句}
```

**回滚SQL**：

```sql
-- {回滚说明}
{回滚SQL语句}
```

---

## 3. 代码变更

### 3.1 新增文件

| 文件路径 | 说明 |
|---------|------|
| {文件路径} | {文件说明} |

### 3.2 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| {文件路径} | {修改说明} |

### 3.3 关键代码片段

```java
// {代码说明}
{关键代码}
```

---

## 4. 测试验证

### 4.1 编译验证

```bash
# 编译命令
{编译命令}

# 编译结果
{编译结果：成功/失败}
```

### 4.2 功能验证

| 测试场景 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|---------|------|
| {场景1} | {步骤} | {预期} | {实际} | {通过/失败} |
| {场景2} | {步骤} | {预期} | {实际} | {通过/失败} |

### 4.3 接口验证

```bash
# 接口测试命令
curl -X {方法} http://localhost:{端口}{接口路径} \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{请求体}'

# 响应结果
{响应JSON}
```

---

## 5. 遇到的问题及解决方案

| 问题描述 | 原因分析 | 解决方案 | 状态 |
|---------|---------|---------|------|
| {问题描述} | {原因} | {方案} | {已解决/待解决} |

---

## 6. 遗留问题

- {遗留问题1，如有}
- {遗留问题2，如有}

---

## 7. 下一步计划

- {下一步要开发的功能点}
- {需要协调的事项}

---

> **项目经理审查**
>
> | 审查项 | 审查结果 |
> |--------|---------|
> | 代码文件存在性 | {通过/不通过} |
> | 代码质量（D08规范） | {通过/不通过} |
> | 功能正确性 | {通过/不通过} |
> | 文档完整性 | {通过/不通过} |
> | Git提交规范性 | {通过/不通过} |
> | **综合审查结论** | **{通过/驳回重做}** |
