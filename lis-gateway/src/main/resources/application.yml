server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: lis-gateway
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:DEFAULT_GROUP}
      config:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:DEFAULT_GROUP}
        file-extension: yaml
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: lis-auth
          uri: lb://lis-auth
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=0

        - id: lis-user
          uri: lb://lis-user
          predicates:
            - Path=/system/user/**
          filters:
            - StripPrefix=0

        - id: lis-dept
          uri: lb://lis-user
          predicates:
            - Path=/system/dept/**
          filters:
            - StripPrefix=0

        - id: lis-role
          uri: lb://lis-user
          predicates:
            - Path=/system/role/**
          filters:
            - StripPrefix=0

        - id: lis-menu
          uri: lb://lis-user
          predicates:
            - Path=/system/menu/**
          filters:
            - StripPrefix=0

        - id: lis-dict
          uri: lb://lis-user
          predicates:
            - Path=/system/dict/**
          filters:
            - StripPrefix=0

        - id: lis-specimen
          uri: lb://lis-specimen
          predicates:
            - Path=/api/specimen/**
          filters:
            - StripPrefix=0

        - id: lis-test-item
          uri: lb://lis-specimen
          predicates:
            - Path=/api/test-item/**
          filters:
            - StripPrefix=0

        - id: lis-department
          uri: lb://lis-specimen
          predicates:
            - Path=/api/department/**
          filters:
            - StripPrefix=0

        - id: lis-report
          uri: lb://lis-report
          predicates:
            - Path=/api/report/**
          filters:
            - StripPrefix=0

        - id: lis-result
          uri: lb://lis-report
          predicates:
            - Path=/api/result/**
          filters:
            - StripPrefix=0

        - id: lis-critical-value
          uri: lb://lis-report
          predicates:
            - Path=/api/critical-value/**
          filters:
            - StripPrefix=0

        - id: lis-equipment
          uri: lb://lis-equipment
          predicates:
            - Path=/equipment/**
          filters:
            - StripPrefix=0

        - id: lis-hl7
          uri: lb://lis-hl7
          predicates:
            - Path=/hl7/**
          filters:
            - StripPrefix=0

        - id: lis-statistics
          uri: lb://lis-statistics
          predicates:
            - Path=/statistics/**
          filters:
            - StripPrefix=0

        - id: lis-ai
          uri: lb://lis-ai
          predicates:
            - Path=/ai/**
          filters:
            - StripPrefix=0

  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

jwt:
  secret: ${JWT_SECRET:lis-backend-jwt-secret-key-2024-very-long-secret-key-for-security}
  expiration: 86400000
  header: Authorization
  prefix: Bearer

gateway:
  white-list:
    - /api/auth/login
    - /api/auth/register
    - /api/auth/captcha
    - /api/auth/refresh
    - /doc.html
    - /webjars/**
    - /swagger-resources/**
    - /v2/api-docs
    - /v3/api-docs/**
    - /actuator/health

logging:
  level:
    com.lis.gateway: debug
    org.springframework.cloud.gateway: debug
