# Agent-00 阶段1任务下达

## 项目信息
- **阶段**：阶段1 - 基础设施（Agent-00）
- **下达日期**：2026-04-08
- **项目经理**：AI项目经理Agent
- **Agent**：Agent-00（架构师）
- **任务分支**：feature/common-infra

## 项目根目录
```
D:\ReStart\FINALWORK
```

## 背景说明

项目已完成基础代码构建，包含380个Java文件分布在10个微服务模块中。PM已完成阶段0初始化，发现并修复了11处编译阻断问题（commit: cfa3a01）。

当前状态：
- `mvn clean compile` → BUILD SUCCESS（407个class）
- 所有10个模块jar已生成
- Git管控体系已建立（develop + 9个feature分支）

## 你的任务

请在 `feature/common-infra` 分支上，完成以下工作：

### 任务一：lis-common公共模块完善

**已有**：18个Java文件
**需验证/补充**：

1. `Result<T>` 统一响应包装类（需验证是否存在且符合规范）
2. `GlobalExceptionHandler` 全局异常处理（需验证是否处理以下异常类型）：
   - BusinessException（自定义业务异常）
   - MethodArgumentNotValidException（参数校验异常）
   - AccessDeniedException（权限不足）
   - Exception（兜底异常）
3. `JwtUtils` JWT工具类（需验证generateToken/parseToken/validateToken方法）
4. `RedisUtils` Redis工具类（需验证常用方法）
5. `BusinessException` + `AuthException` 自定义异常（需验证构造器完整）
6. `CommonConstants` 公共常量类
7. `@RequirePermission`、`@RateLimiter`、`@OperationLog` 自定义注解

**验收标准**：
- 所有类可通过编译
- 提供完整的Javadoc注释
- 代码风格符合编码规范

### 任务二：lis-gateway API网关完善

**已有**：8个Java文件
**需验证/补充**：

1. 路由配置（需验证是否包含所有8个微服务的路由）
2. `AuthGlobalFilter` JWT鉴权过滤器（需验证）：
   - 从Authorization头提取Bearer Token
   - 白名单路径放行（/api/auth/**、/actuator/**）
   - 调用JwtUtils.validateToken()
   - Token无效返回401
   - Token即将过期添加X-Token-Expiring头
3. CORS跨域配置
4. Sentinel限流配置
5. `AccessLogGlobalFilter` 访问日志过滤器

**验收标准**：
- 路由可正确转发到各下游服务
- JWT鉴权过滤器正确工作
- 跨域配置正确

### 任务三：lis-auth认证服务完善

**已有**：14个Java文件
**需验证/补充**：

1. `POST /api/auth/login` 登录接口
2. `POST /api/auth/refresh` Token刷新接口
3. `GET /api/auth/userinfo` 用户信息接口
4. `POST /api/auth/logout` 登出接口

**验收标准**：
- 所有4个接口功能正确
- JWT Token生成/验证正常
- Redis存储Token信息正常

### 任务四：数据库初始化脚本验证

**已有**：8个SQL脚本
**需验证**：
1. 数据库创建语句是否完整（7个业务数据库）
2. 建表语句是否包含公共审计字段（id/create_by/create_time/update_by/update_time/deleted/remark）
3. 初始化数据是否包含管理员账户

### 任务五：Docker部署配置完善

**已有**：
- docker-compose.yml
- 后端Dockerfile
- 前端Dockerfile

**需补充**：
1. Nginx配置文件（前端静态资源服务 + API反向代理）
2. MySQL初始化脚本挂载

### 任务六：模块集成验证

1. 所有微服务在 `bootstrap.yml` 中正确配置Nacos服务发现
2. Feign客户端定义（UserFeignClient等）正确
3. 统一日志格式配置
4. 跨服务异常处理机制

## 禁止事项

- 禁止直接在develop或main分支上提交代码
- 禁止提交包含明文密码的配置文件
- 禁止删除已有功能代码（只能补充和修复）
- 禁止提交编译不通过的代码

## 输出要求

完成所有任务后，请输出：

1. **开发报告**：`D:\ReStart\FINALWORK\docs\process\Agent-00\阶段1开发报告.md`
2. **变更日志**：`D:\ReStart\FINALWORK\docs\process\Agent-00\CHANGELOG.md`
3. **Git提交**：在feature/common-infra分支提交所有变更

## 技术栈参考

- JDK 17
- Spring Boot 2.7.18
- Spring Cloud Alibaba 2022.0.0.0
- Nacos 2.2.3
- Spring Cloud Gateway 3.1.x
- Sentinel 1.8.6
- MySQL 8.0
- Redis 7.0
- JWT (jjwt 0.11.5)
- MyBatis-Plus 3.5.5
- Docker 24.x
- Nginx 1.24+

---

*本任务书由AI项目经理下达 | 下达时间：2026-04-08*
