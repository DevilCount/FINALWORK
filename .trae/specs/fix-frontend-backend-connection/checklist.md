# Checklist

## 登录验证码移除验证

* [x] 登录页面无验证码输入框

* [x] 登录页面无验证码图片区域

* [x] 登录请求不包含 code 和 uuid 字段

* [ ] 无验证码时登录功能正常（需后端运行后验证）

## 前后端连通性验证

* [x] 前端请求 /api/auth/login 路径链路正确（baseURL→proxy→gateway→auth服务）

* [x] Gateway 正确路由 /auth/\*\* 到 Auth 服务

* [x] Auth 服务 /auth/login 端点存在且映射正确

* [x] 前端 LoginResult 类型与后端 LoginVO 字段完全匹配

* [ ] 前端能正确解析登录响应并存储 Token（需后端运行后验证）

## 登录流程端到端验证

* [ ] 输入正确用户名密码后登录成功（需后端运行后验证）

* [ ] 登录成功后跳转到首页 /dashboard/index（需后端运行后验证）

* [ ] 登录失败时显示错误提示（需后端运行后验证）

* [ ] 登录后获取用户信息成功（需后端运行后验证）

* [ ] 登录后动态路由生成成功（需后端运行后验证）

