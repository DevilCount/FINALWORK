<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lis.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.lis.user.entity.UserDO">
        <id column="id" property="id"/>
        <result column="dept_id" property="deptId"/>
        <result column="user_name" property="userName"/>
        <result column="nick_name" property="nickName"/>
        <result column="user_type" property="userType"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="gender" property="gender"/>
        <result column="avatar" property="avatar"/>
        <result column="password" property="password"/>
        <result column="status" property="status"/>
        <result column="del_flag" property="delFlag"/>
        <result column="login_ip" property="loginIp"/>
        <result column="login_date" property="loginDate"/>
        <result column="pwd_update_time" property="pwdUpdateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <select id="selectByUserName" resultMap="UserResultMap">
        SELECT * FROM sys_user WHERE user_name = #{userName} AND del_flag = 0
    </select>

    <select id="selectUserList" resultMap="UserResultMap">
        SELECT * FROM sys_user WHERE del_flag = 0
        <if test="deptId != null">
            AND dept_id = #{deptId}
        </if>
        <if test="userName != null and userName != ''">
            AND user_name LIKE CONCAT('%', #{userName}, '%')
        </if>
        <if test="nickName != null and nickName != ''">
            AND nick_name LIKE CONCAT('%', #{nickName}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND phone LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="userType != null and userType != ''">
            AND user_type = #{userType}
        </if>
        ORDER BY create_time DESC
    </select>

    <update id="updatePassword">
        UPDATE sys_user SET password = #{password}, pwd_update_time = NOW(), update_time = NOW() WHERE id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE sys_user SET status = #{status}, update_time = NOW() WHERE id = #{userId}
    </update>

    <update id="updateLoginInfo">
        UPDATE sys_user SET login_ip = #{loginIp}, login_date = NOW(), update_time = NOW() WHERE id = #{userId}
    </update>

</mapper>
